apiVersion: batch/v1
kind: Job
metadata:
  name: batch-check-job
spec:
  template:
    metadata:
      name: batch-check-job
    spec:
      containers:
      - name: batch-check
        image: batch-check:latest
        imagePullPolicy: Never
        # command:
        #  - "bin/sh"
        #  - "-c"
        #  - "htop"
      restartPolicy: Never
      