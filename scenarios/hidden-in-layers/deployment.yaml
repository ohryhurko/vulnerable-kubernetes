apiVersion: batch/v1
kind: Job
metadata:
  name: hidden-in-layers
spec:
  template:
    metadata:
      name: hidden-in-layers
    spec:
      containers:
      - name: hidden-in-layers
        image: hidden-in-layers:latest
        imagePullPolicy: Never
        # command:
        #  - "bin/sh"
        #  - "-c"
        #  - "htop"
      restartPolicy: Never
